{
  /* <div className={styles.testDiv}>
        {filteredData?.map((item, i) => {
          const itemDate = moment(item.date, "YYYYMMDD");
          if (days && days.isSame(itemDate, "day")) {
            return (
              <div
                key={i}
                className={`${styles.listItem} ${styles[item.category]}`}
              >
                {item.data}
              </div>
            );
          }
        })}
      </div> */
}
{
  /* <div className={styles.testDiv}>
        {filteredData?.map((item, i) => {
          if (item.category === "broadcast") {
            return (
              <div
                key={i}
                className={`${styles.listItem} ${styles[item.category]}`}
              >
                {item.data}
              </div>
            );
          } else if (item.category === "release") {
            return (
              <div
                key={i}
                className={`${styles.listItem} ${styles[item.category]}`}
              >
                {item.data}
              </div>
            );
          } else if (item.category === "buy") {
            return (
              <div
                key={i}
                className={`${styles.listItem} ${styles[item.category]}`}
              >
                {item.data}
              </div>
            );
          } else if (item.category === "congrats") {
            return (
              <div
                key={i}
                className={`${styles.listItem} ${styles[item.category]}`}
              >
                {item.data}
              </div>
            );
          } else if (item.category === "event") {
            return (
              <div
                key={i}
                className={`${styles.listItem} ${styles[item.category]}`}
              >
                {item.data}
              </div>
            );
          } else {
            return (
              <div
                key={i}
                className={`${styles.listItem} ${styles[item.category]}`}
              >
                {item.data}
              </div>
            );
          }
        })}
      </div> */
}
